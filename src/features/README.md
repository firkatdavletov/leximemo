# Features Layer

Слой для бизнес-фич (auth/decks/cards/review).

Текущие фичи:

- `auth` — формы логина/регистрации;
- `decks` — формы и действия колод;
- `cards` — формы и действия карточек;
- `review` — режим обучения и алгоритм интервального повторения.
